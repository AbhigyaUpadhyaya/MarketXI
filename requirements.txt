# Minimum versions pinned to the verified dev environment (Python 3.12, venv/).
# Floors use >= so newer compatible releases are accepted.
requests>=2.34.2
pandas>=3.0.5
scikit-learn>=1.9.0
matplotlib>=3.11.1
numpy>=2.5.2
duckdb>=1.5.5
joblib>=1.6.0
pytest>=9.1.1
# Required by app.py (FastAPI backend + static server); were previously missing.
fastapi>=0.141.1
uvicorn>=0.52.4
